{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  
  "scripts": {
    "test": "jest",
    "build": "if exist dist rmdir /s /q dist && mkdir dist && copy index.js dist\\"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
